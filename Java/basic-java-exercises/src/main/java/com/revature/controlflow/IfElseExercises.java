package com.revature.controlflow;

public class IfElseExercises {
    public static void main(String[] args) {
        /*
        1. Create a boolean variable called myBoolean and assign it a boolean value.
        Update the below code so that the print statement appears in the console.
        */
        if (false) {
            System.out.println("If you're seeing this in the console, Exercise 1 is complete!");
        }

        /*
        2. Create two variables named num1 and num2. Assign them both numeric values.
        Update the below if statement (using both variables!) so the code inside the block executes.
        (HINT: You'll need to use one or more logical operators)
         */
        if(false) {
            System.out.println("If you're seeing this in the console, Exercise 2 is complete!");
        }


        /*
        3. Create two String variables str1 and str2. Write an if else statement that will either print str1 to the
        console if its length is greater than 5, or print the str2 in all caps.
         */


        /*
        4. Create two int variables. Write a combination of if else if else statements to determine:
            a. if a number is even
                a2. if the number is greater than 100 and less than 140
                    then print that number to the console
                    else print a random string
            b. else if a number is odd
                b2. if the number is less than 100 and greater than 50
                    then print that number to the console
                    else print

         */





        /*
        5. Create any variable and use it in a ternary operation
         */
    }

}
